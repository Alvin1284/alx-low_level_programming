#include<stdio.h>
#include<string.h>
/**
 *main-prints string
 *followed by a new line
 * Return: always (0)
 */
int main(void)
{printf("\with proper grammar, but the outcome is a piece of art,");
return (0);
}
